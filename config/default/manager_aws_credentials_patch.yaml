apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloud-burst-controller
  namespace: cloud-burst-system
spec:
  template:
    spec:
      containers:
        - name: manager
          envFrom:
            - secretRef:
                name: aws-burst-credentials
