apiVersion: v1
kind: Service
metadata:
  labels:
    control-plane: controller-manager
    app.kubernetes.io/name: cloud-burst-controller
    app.kubernetes.io/managed-by: kustomize
  name: cloud-burst-controller-metrics-service
  namespace: cloud-burst-system
spec:
  ports:
  - name: https
    port: 8443
    protocol: TCP
    targetPort: 8443
  selector:
    control-plane: controller-manager
    app.kubernetes.io/name: cloud-burst-controller
