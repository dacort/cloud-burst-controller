---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: burstnodepools.burst.homelab.dev
spec:
  group: burst.homelab.dev
  names:
    kind: BurstNodePool
    listKind: BurstNodePoolList
    plural: burstnodepools
    singular: burstnodepool
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.cloud
      name: Cloud
      type: string
    - jsonPath: .spec.scaling.maxNodes
      name: Max
      type: integer
    - jsonPath: .status.activeNodes
      name: Active
      type: integer
    - jsonPath: .status.pendingNodes
      name: Pending
      type: integer
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BurstNodePool is the Schema for the burstnodepools API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BurstNodePoolSpec defines the desired state of BurstNodePool.
            properties:
              aws:
                description: AWS-specific configuration. Required when cloud is "aws".
                properties:
                  ami:
                    description: AMI ID for Talos instances.
                    type: string
                  instanceType:
                    description: EC2 instance type.
                    type: string
                  region:
                    description: AWS region for EC2 instances.
                    type: string
                  securityGroupIds:
                    description: Security group IDs.
                    items:
                      type: string
                    type: array
                  subnetId:
                    description: Subnet ID for instance placement.
                    type: string
                  tags:
                    additionalProperties:
                      type: string
                    description: Additional EC2 tags.
                    type: object
                  volumeSize:
                    default: 30
                    description: Root volume size in GB.
                    format: int32
                    type: integer
                  volumeType:
                    default: gp3
                    description: Root volume type.
                    type: string
                required:
                - ami
                - instanceType
                - region
                - securityGroupIds
                - subnetId
                type: object
              cloud:
                description: Cloud provider discriminator. Only "aws" is supported
                  in v1.
                enum:
                - aws
                type: string
              matchRules:
                description: Rules for matching unschedulable pods to this pool.
                properties:
                  nodeAffinityLabels:
                    additionalProperties:
                      type: string
                    description: Node affinity labels the pod must request.
                    type: object
                  resources:
                    description: Resource requests the pod must have.
                    items:
                      properties:
                        operator:
                          type: string
                        resourceName:
                          type: string
                      required:
                      - operator
                      - resourceName
                      type: object
                    type: array
                  tolerations:
                    description: Tolerations the pod must have.
                    items:
                      properties:
                        key:
                          type: string
                        operator:
                          type: string
                        value:
                          type: string
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                type: object
              nodeLabels:
                additionalProperties:
                  type: string
                description: Labels applied to burst nodes when they register.
                type: object
              nodeTaints:
                description: Taints applied to burst nodes when they register.
                items:
                  properties:
                    effect:
                      type: string
                    key:
                      type: string
                    value:
                      type: string
                  required:
                  - effect
                  - key
                  type: object
                type: array
              scaling:
                description: Scaling parameters.
                properties:
                  bootTimeout:
                    default: 5m
                    description: Max time to wait for a node to become Ready.
                    type: string
                  cooldownPeriod:
                    default: 5m
                    description: How long a node must be idle before termination.
                    type: string
                  maxNodes:
                    description: Maximum number of nodes this pool can provision.
                    format: int32
                    minimum: 0
                    type: integer
                required:
                - maxNodes
                type: object
              talos:
                description: Talos machine config reference.
                properties:
                  machineConfigKey:
                    default: worker.yaml
                    description: Key within the Secret.
                    type: string
                  machineConfigSecret:
                    description: Name of the Secret containing the Talos machine config.
                    type: string
                required:
                - machineConfigSecret
                type: object
            required:
            - cloud
            - matchRules
            - scaling
            - talos
            type: object
          status:
            description: BurstNodePoolStatus defines the observed state of BurstNodePool.
            properties:
              activeNodes:
                description: Number of nodes in Running state.
                format: int32
                type: integer
              conditions:
                description: Standard conditions.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              nodes:
                description: Individual node statuses.
                items:
                  properties:
                    instanceId:
                      description: Cloud provider instance ID.
                      type: string
                    lastPodFinished:
                      description: When the last non-daemonset pod finished on this
                        node.
                      format: date-time
                      type: string
                    launchedAt:
                      description: When the instance was launched.
                      format: date-time
                      type: string
                    name:
                      description: Kubernetes node name.
                      type: string
                    readyAt:
                      description: When the node became Ready.
                      format: date-time
                      type: string
                    state:
                      description: Current lifecycle state.
                      enum:
                      - Pending
                      - Running
                      - Cordoned
                      - Draining
                      - Terminating
                      type: string
                  required:
                  - instanceId
                  - launchedAt
                  - name
                  - state
                  type: object
                type: array
              pendingNodes:
                description: Number of nodes in Pending state.
                format: int32
                type: integer
            required:
            - activeNodes
            - pendingNodes
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
